export enum EEnvironment {
  DEVELOPMENT = 'development',
  TEST = 'test',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export enum EStatus {
  ACTIVED = 'actived',
  UNACTIVE = 'unactive',
}
